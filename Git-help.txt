$ git init - Ёта команда создаЄт в текущем каталоге 
			новый подкаталог с именем .git содержащий все необходимые 
			файлы репозитори€ Ч основу Git-репозитори€.
			
$ git clone https://github.com/FujiF/Test_repo.git -  лонирование репозитори€

$ git add - индексирует файлы в рабочего каталога

$ git commit -a -m(message 'hello')- заставл€ет Git автоматически индексировать
каждый уже отслеживаемый на момент коммита файл, позвол€€ вам обойтись без git add.

$ git rm - удал€ет файл из вашего рабочего каталога

$ git rm --cached readme.txt -  вы можете захотеть оставить файл на винчестере,
 и убрать его из-под бдительного ока Git'а
 
 $ git mv file_from file_to - переименовать файл


			
